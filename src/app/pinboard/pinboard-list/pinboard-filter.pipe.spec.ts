import { PinboardFilterPipe } from './pinboard-filter.pipe';

describe('PinboardFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PinboardFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
